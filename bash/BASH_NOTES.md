# To use this bashrc config:

Include these configurations by appending the following lines to the local .bashrc file:  

    if [ -f ~/portable_configs/shell/bashrc ]; then  
        ~/portable_configs/shell/bashrc  
    fi

